# PARALLEL SOFT (obscure Pages)

Private-ish public Pages host for AMP Soft Mess + Soft BD Hub.

- Soft storage prefixes only (`amp_soft_mess_`, `amp_soft_bdhub_`)
- Live mango-island Hub is untouched
- Sign in with your AMP Supabase Auth email (not Playhouse forced-Mike)
- `noindex` via robots.txt + meta

Exec Soft doors: `mess.html`, `bd.html`
